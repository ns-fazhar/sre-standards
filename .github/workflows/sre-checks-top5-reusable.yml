name: SRE Top 5 Checks (Reusable)

on:
  workflow_call:
    inputs:
      fail_on_issues:
        description: 'Fail workflow if critical issues found'
        type: boolean
        default: false
      run_npi:
        description: 'Run NPI checks (for feature branches)'
        type: boolean
        default: true

jobs:
  # ============================================================================
  # CONTINUOUS VALIDATION: Run on ALL PRs (entire codebase)
  # ============================================================================

  sre-checks:
    name: SRE Checks (Reliability & Resilience)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout service repository
        uses: actions/checkout@v4

      - name: Checkout SRE standards (central repo)
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/sre-standards
          path: .sre-standards
          ref: main

      - name: Show SRE standards version
        run: |
          echo "📦 Using SRE standards version: $(cat .sre-standards/VERSION)"
          echo ""

      - name: Run SRE reliability checks
        id: checks
        continue-on-error: true
        run: |
          .sre-standards/generated/check-sre-top5.sh 2>&1 | tee check-output.txt
          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          exit 0

      - name: Post PR comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('check-output.txt', 'utf8');
            const exitCode = '${{ steps.checks.outputs.exit_code }}';
            const sreVersion = fs.readFileSync('.sre-standards/VERSION', 'utf8').trim();

            const status = exitCode === '0' ? '✅ PASSED' : '❌ ISSUES FOUND';

            const criticalMatch = output.match(/(\d+) critical/i);
            const warningsMatch = output.match(/(\d+) warning/i);
            const critical = criticalMatch ? criticalMatch[1] : '0';
            const warnings = warningsMatch ? warningsMatch[1] : '0';

            const body = `## 🔧 SRE Checks (Reliability & Resilience): ${status}

            **SRE Standards Version**: \`${sreVersion}\`
            **Summary**: ${critical} critical issue(s), ${warnings} warning(s)

            <details>
            <summary>📄 Click to see detailed results</summary>

            \`\`\`
            ${output}
            \`\`\`

            </details>

            ---
            🤖 Powered by [sre-standards](https://github.com/${context.repo.owner}/sre-standards) v${sreVersion}
            `;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('SRE Checks (Reliability & Resilience)')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Check for critical issues
        if: inputs.fail_on_issues
        run: |
          EXIT_CODE="${{ steps.checks.outputs.exit_code }}"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ SRE reliability checks failed"
            exit 1
          fi

  operability-checks:
    name: Operability Checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout service repository
        uses: actions/checkout@v4

      - name: Checkout SRE standards
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/sre-standards
          path: .sre-standards
          ref: main

      - name: Run operability checks
        id: checks
        continue-on-error: true
        run: |
          .sre-standards/generated/check-operability-top5.sh 2>&1 | tee check-output.txt
          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          exit 0

      - name: Post PR comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('check-output.txt', 'utf8');
            const exitCode = '${{ steps.checks.outputs.exit_code }}';
            const sreVersion = fs.readFileSync('.sre-standards/VERSION', 'utf8').trim();

            const status = exitCode === '0' ? '✅ PASSED' : '❌ ISSUES FOUND';

            const criticalMatch = output.match(/(\d+) critical/i);
            const warningsMatch = output.match(/(\d+) warning/i);
            const critical = criticalMatch ? criticalMatch[1] : '0';
            const warnings = warningsMatch ? warningsMatch[1] : '0';

            const body = `## ⚙️ Operability Checks: ${status}

            **SRE Standards Version**: \`${sreVersion}\`
            **Summary**: ${critical} critical issue(s), ${warnings} warning(s)

            <details>
            <summary>📄 Click to see detailed results</summary>

            \`\`\`
            ${output}
            \`\`\`

            </details>

            ---
            🤖 Powered by [sre-standards](https://github.com/${context.repo.owner}/sre-standards) v${sreVersion}
            `;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('Operability Checks')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Check for critical issues
        if: inputs.fail_on_issues
        run: |
          EXIT_CODE="${{ steps.checks.outputs.exit_code }}"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ Operability checks failed"
            exit 1
          fi

  observability-checks:
    name: Observability Checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout service repository
        uses: actions/checkout@v4

      - name: Checkout SRE standards
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/sre-standards
          path: .sre-standards
          ref: main

      - name: Run observability checks
        id: checks
        continue-on-error: true
        run: |
          .sre-standards/generated/check-observability-top5.sh 2>&1 | tee check-output.txt
          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          exit 0

      - name: Post PR comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('check-output.txt', 'utf8');
            const exitCode = '${{ steps.checks.outputs.exit_code }}';
            const sreVersion = fs.readFileSync('.sre-standards/VERSION', 'utf8').trim();

            const status = exitCode === '0' ? '✅ PASSED' : '❌ ISSUES FOUND';

            const criticalMatch = output.match(/(\d+) critical/i);
            const warningsMatch = output.match(/(\d+) warning/i);
            const critical = criticalMatch ? criticalMatch[1] : '0';
            const warnings = warningsMatch ? warningsMatch[1] : '0';

            const body = `## 📊 Observability Checks: ${status}

            **SRE Standards Version**: \`${sreVersion}\`
            **Summary**: ${critical} critical issue(s), ${warnings} warning(s)
            **Stack**: Prometheus (metrics) + SUMO Logic (logging)

            <details>
            <summary>📄 Click to see detailed results</summary>

            \`\`\`
            ${output}
            \`\`\`

            </details>

            ---
            🤖 Powered by [sre-standards](https://github.com/${context.repo.owner}/sre-standards) v${sreVersion}
            `;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('Observability Checks')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Check for critical issues
        if: inputs.fail_on_issues
        run: |
          EXIT_CODE="${{ steps.checks.outputs.exit_code }}"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ Observability checks failed"
            exit 1
          fi

  # ============================================================================
  # FEATURE VALIDATION: Run ONLY on feature branches (changed files only)
  # ============================================================================

  npi-checks:
    name: NPI Checks (New Product Introduction)
    runs-on: ubuntu-latest
    # Only run if: 1) input enabled, 2) pull request, 3) not merging to main/master
    if: |
      inputs.run_npi &&
      github.event_name == 'pull_request' &&
      github.head_ref != 'main' &&
      github.head_ref != 'master'
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout service repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for git diff

      - name: Checkout SRE standards
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/sre-standards
          path: .sre-standards
          ref: main

      - name: Run NPI checks
        id: checks
        continue-on-error: true
        env:
          BASE_BRANCH: ${{ github.base_ref }}
        run: |
          .sre-standards/generated/check-npi-top5.sh 2>&1 | tee check-output.txt
          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          exit 0

      - name: Post PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('check-output.txt', 'utf8');
            const exitCode = '${{ steps.checks.outputs.exit_code }}';
            const sreVersion = fs.readFileSync('.sre-standards/VERSION', 'utf8').trim();

            const status = exitCode === '0' ? '✅ PASSED' : '❌ ISSUES FOUND';

            const criticalMatch = output.match(/(\d+) critical/i);
            const warningsMatch = output.match(/(\d+) warning/i);
            const critical = criticalMatch ? criticalMatch[1] : '0';
            const warnings = warningsMatch ? warningsMatch[1] : '0';

            const body = `## 🚀 NPI Checks (New Product Introduction): ${status}

            **SRE Standards Version**: \`${sreVersion}\`
            **Feature Branch**: \`${context.payload.pull_request.head.ref}\`
            **Base Branch**: \`${context.payload.pull_request.base.ref}\`
            **Summary**: ${critical} critical issue(s), ${warnings} warning(s)

            > ℹ️ NPI checks validate **new code only** (changed files in this PR)

            <details>
            <summary>📄 Click to see detailed results</summary>

            \`\`\`
            ${output}
            \`\`\`

            </details>

            ${exitCode === '0'
              ? '✅ All NPI checks passed! New features meet SRE requirements.'
              : `⚠️ Found issues in new feature code.

            **Critical checks failed:**
            - SQL Injection Prevention
            - Feature Flag Detection
            - Database Migrations
            - API Breaking Changes
            - Test Coverage

            **Fix before merge to maintain production quality.**
            `}

            ---
            🤖 Powered by [sre-standards](https://github.com/${context.repo.owner}/sre-standards) v${sreVersion}
            `;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('NPI Checks (New Product Introduction)')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Check for critical issues
        if: inputs.fail_on_issues
        run: |
          EXIT_CODE="${{ steps.checks.outputs.exit_code }}"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ NPI checks failed - new feature has critical issues"
            exit 1
          fi
